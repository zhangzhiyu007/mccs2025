# MCCS 系统性审计与修复路线图（v1）

> 目标：把“零散缺陷修复”升级为“可度量、可回归、可交付”的系统性治理。

## 1. 审计框架（6 条主路径）

### P1. 生命周期与停机可靠性（Lifecycle Reliability）
**问题定义**：系统能否在 `SIGTERM/SIGINT` 下有序退出，避免残留线程/句柄/看门狗状态。  
**核心检查项**：
- 主循环是否可退出（退出条件/信号处理）。
- `Init()` 与 `Uninit()` 是否一一对称。
- 启动中途失败是否“逆序回滚”。

**重点文件**：`src/main.cpp`、`src/Manager.cpp`。

---

### P2. 线程框架契约一致性（Threading Contract）
**问题定义**：线程创建/停止/等待模型是否自洽，避免系统级偶发崩溃与卡死。  
**核心检查项**：
- `detach` 与 `join` 是否混用。
- `Start()` 是否处理创建失败。
- `Stop()` 是否具备可中断等待（避免长阻塞）。

**重点文件**：`src/etc/socket/Thread.h`、`src/etc/socket/Thread.cpp`、各 `*Thread.cpp`。

---

### P3. 资源所有权与隔离（Ownership & Isolation）
**问题定义**：连接、句柄、对象释放责任是否清晰，是否存在跨实例互相踩踏。  
**核心检查项**：
- DB 连接是否为“实例私有”而不是“全局共享”。
- 网络对象（TcpClient/Socket）是否有唯一 owner。
- 文件句柄/互斥量/条件变量是否具备 init-destroy 对称性。

**重点文件**：`src/db/Db.cpp`、`src/io/socket/*`、`src/db/HisDbSave.cpp`、`src/util/*`。

---

### P4. 故障恢复与退避策略（Recovery & Backoff）
**问题定义**：掉线、超时、设备失联时是否可恢复，是否会造成停机路径阻塞。  
**核心检查项**：
- 重连退避是否支持“快速响应 Stop”。
- 重试上限与降级策略是否可配置。
- 错误路径是否统一返回错误码并记录上下文。

**重点文件**：`src/io/socket/SocketThread.cpp`、`src/net/*`、`src/io/com/Com.cpp`。

---

### P5. 可观测性与可运维性（Observability）
**问题定义**：线上问题是否可快速定位；是否有关键健康指标。  
**核心检查项**：
- 关键阶段日志（启动、重连、停机、回滚）是否完整。
- 关键指标（线程存活、连接状态、队列长度）是否可导出。
- 日志级别与日志格式是否统一。

**重点文件**：`src/util/Util.cpp`、`src/main.cpp`、`src/Manager.cpp`。

---

### P6. 构建可重复性与发布门禁（Build Reproducibility）
**问题定义**：不同环境构建结果是否稳定一致，告警是否可控。  
**核心检查项**：
- host/cross 构建矩阵是否全绿。
- `USE_API_LIBS` 两条路径是否持续可编译。
- warning baseline 是否稳定（避免“修一处坏一片”）。

**重点文件**：`src/Makefile`。

---

## 2. 执行节奏（建议按周）

### 阶段 A（P0，先做）
1. 完成 `main` 信号退出与清理路径打通。  
2. 完成 `Manager` 启动失败回滚 + 关闭对称性。  
3. 明确 `Thread` 基类的唯一模型（joinable 或 detached 二选一）。

**交付标准**：
- 可重复触发“优雅停机”；
- 停机耗时可测（目标 <= 5s，特殊模块注明例外）。

### 阶段 B（P1）
1. DB 连接所有权改造（去全局共享句柄）。
2. 条件变量/互斥量生命周期清理（初始化/销毁对称）。
3. 网络重连退避可中断化（Stop 优先）。

### 阶段 C（P1/P2）
1. 大批量设备协议读取边界检查统一化（size/长度防护模板）。
2. signed/unsigned 规范化处理。
3. 统一错误路径与日志上下文。

---

## 3. 每个问题的统一记录模板

```
[ID] 例：LIFECYCLE-001
[级别] P0/P1/P2
[现象] 线上表现或潜在故障
[触发条件] 并发/异常输入/停机场景
[根因] 代码与设计层根因
[影响范围] 模块、线程、设备类型
[修复方案] 最小修复 + 长期方案
[验证] 编译、回归、压力、故障注入
[回滚方案] 若修复引发副作用
```

---

## 4. 本项目的首批问题候选（建议立项）

1. `main` 无退出路径导致清理代码不可达。  
2. `Manager` 启动失败无逆序回滚，且部分线程生命周期不对称。  
3. `Thread` 基类存在 `detach/join` 契约冲突。  
4. `Db` 采用全局连接句柄，实例隔离性不足。  
5. `HisDbSaveThread` 条件变量生命周期需专项核验。  
6. socket 重连长睡眠影响 Stop 响应时延。

---

## 5. 评审与验收机制

- 每次修复必须附：
  - “修复前后行为对照”；
  - “最小复现步骤”；
  - “失败注入结果（如断网/设备不响应）”；
  - “停机耗时与资源释放截图/日志”。
- 每周维护一份风险燃尽表：新增、关闭、遗留、回归。

---

## 6. 注意事项

- 优先修底座（生命周期/线程/资源），再修业务协议细节。  
- 避免一次性大改，按“可回滚的小步提交”推进。  
- 对于嵌入式现场问题，先保证“可停机、可恢复、可定位”。
